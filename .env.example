# Optional: public URL of the separately hosted API server.
# Leave empty for local development when the API is served from the same origin.
VITE_API_BASE_URL=

# Backend-only variables. Keep real values out of Git.
DATABASE_URL=
SESSION_SECRET=
ADMIN_ACCESS_TOKEN=